<div class="min-h-screen bg-lavender p-4">
  <header class="mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Edit Narration</h1>
    <p class="text-gray-600">for <%= @student.name %></p>
  </header>

  <div class="bg-white rounded-xl p-6 shadow-sm">
    <%= render "form", narration: @narration, student: @student, subjects: @subjects %>
  </div>
</div>
