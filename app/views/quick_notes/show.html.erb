<div class="min-h-screen bg-lavender p-4">
  <header class="mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Quick Note</h1>
    <p class="text-gray-600"><%= @student.name %> - <%= @recording.date.strftime("%b %d, %Y") %></p>
  </header>

  <div class="max-w-lg">
    <%= render partial: "quick_note", locals: { quick_note: @quick_note } %>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to Quick Notes", student_quick_notes_path(@student), class: "text-gray-600 hover:text-gray-800" %>
  </div>
</div>
