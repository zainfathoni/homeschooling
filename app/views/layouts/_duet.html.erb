<%# locals: (weekly_grid:, daily_focus:) %>
<%# Responsive layout: mobile single column, tablet 60/40 split %>
<%# Weekly grid rendered once to avoid duplicate IDs for turbo_stream updates %>

<div class="md:grid md:grid-cols-5 md:gap-4 md:h-[calc(100vh-4rem)] md:p-4">
  <%# Weekly grid: full width on mobile, 60% on tablet+ %>
  <div class="md:col-span-3 md:overflow-auto">
    <%= weekly_grid %>
  </div>

  <%# Daily Focus: hidden on mobile (accessed via bottom nav), 40% on tablet+ %>
  <div class="hidden md:block md:col-span-2 md:overflow-auto">
    <%= turbo_frame_tag "daily_focus" do %>
      <%= daily_focus %>
    <% end %>
  </div>
</div>
