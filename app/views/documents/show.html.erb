<div class="min-h-screen bg-lavender p-4">
  <header class="mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Document</h1>
    <p class="text-gray-600"><%= @student.name %> - <%= @document.subject.name %></p>
  </header>

  <div class="max-w-lg">
    <%= render partial: "document", locals: { document: @document } %>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to Documents", student_documents_path(@student), class: "text-gray-600 hover:text-gray-800" %>
  </div>
</div>
