<div class="bg-white rounded-lg shadow p-6 max-w-md mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 mb-4">Welcome, <%= Current.user.name %>!</h1>
  <p class="text-gray-600 mb-6">You're signed in as <%= Current.user.email %></p>

  <%= button_to "Sign out", logout_path, method: :delete, class: "bg-coral text-white px-4 py-2 rounded-lg hover:opacity-90 transition min-h-[44px]" %>
</div>
