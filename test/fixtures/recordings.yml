text_narration_recording:
  student: one
  date: <%= Date.new(2026, 1, 26) %>
  recordable_type: Narration
  recordable_id: <%= ActiveRecord::FixtureSet.identify(:text_narration) %>

another_text_recording:
  student: one
  date: <%= Date.new(2026, 1, 27) %>
  recordable_type: Narration
  recordable_id: <%= ActiveRecord::FixtureSet.identify(:another_text) %>

quick_note_recording:
  student: one
  date: <%= Date.new(2026, 1, 26) %>
  recordable_type: QuickNote
  recordable_id: <%= ActiveRecord::FixtureSet.identify(:field_trip) %>
